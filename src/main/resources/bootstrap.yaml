spring:
  application:
    name: backend
  cloud:
    kubernetes:
      enabled: true
      config:
        enabled: true
        name: backend
#        namespace: spring
#        enableApi: false
#        enableK8sController: true
#        uri: http://localhost:8080/configmaps
##        namespace: default